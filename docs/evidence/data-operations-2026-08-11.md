# 数据运维验收记录：2026-08-11

## 两条独立链路

- 每天 02:30 UTC 更新五种资产日线，数据校验和整站回归全部通过后才提交。
- 每 6 小时独立读取线上 `https://www.mybtcbox.com/data/health.json`，用于发现仓库成功但部署、缓存或公开文件异常的情况。
- 两条链路使用不同的 GitHub Issue 标题，失败时创建或追加记录，恢复后留言并关闭。

## 机器检查

`scripts/check-data-health.js` 验证：

- HTTP 成功且返回 `application/json`，防止错误路由返回首页 HTML 却被误认为健康。
- BTC、ETH、SOL、DOGE、BNB 五种资产全部存在。
- 最新完整日线距当前日期不超过 2 个 UTC 日。
- 样本量至少 365 条，SHA-256 为 64 位十六进制值。
- 清单状态为健康，并报告日期不一致和声明延迟不一致的情况。

自动化测试覆盖正常、过期、缺少资产、样本量异常和 SHA-256 损坏。

## 公开状态

- `https://www.mybtcbox.com/status.html` 已部署并返回 HTTP 200。
- 页面展示总体状态、健康资产数、数据截止、清单生成时间和五种资产明细。
- 页面具备加载、正常、降级和读取失败状态。
- 390px 移动端无横向溢出，五种资产均正常呈现，无控制台错误。

## 本次线上检查

2026-08-11 执行 `npm run health:online`：

- 5/5 资产通过。
- 数据截止均为 2026-08-09，延迟 2 个 UTC 日。
- BTC 3,508 条，ETH 3,196 条，SOL 2,313 条，DOGE 3,196 条，BNB 3,196 条。
- 状态页、状态脚本和机器清单均返回正确内容。

## 当前评分

数据运维：8.0/10。

公开可观察性、独立线上巡检、结构化失败原因和恢复手册已经存在，但还没有真实告警 Issue 的完整开启与关闭证据，也没有 30 分钟恢复演练和 30 天可用率记录，因此不超过 8.0。

## 尚缺证据

- 一次受控失败触发真实 GitHub Issue，随后恢复并自动关闭。
- 一次从发现、定位、修复到线上验证的计时恢复演练，耗时不超过 30 分钟。
- 至少 30 天巡检运行历史和可用率汇总。
