# SEO 验收记录：2026-08-11

## 已验证

### 核心工具稳定 URL

- `/tools/` 公开工具目录
- `/tools/btc-drop-history`
- `/tools/btc-rise-history`
- `/tools/btc-volatility-history`
- `/tools/btc-wick-history`
- `/tools/btc-cycle-clock`
- `/tools/btc-conditional-buy-backtest`

工具目录按“历史事件查询”和“周期与策略比较”组织六个任务，并公开共同数据原则。六个工具路由均由 Cloudflare Pages Function 复用同一套真实工具代码，不是关键词落地页。历史事件路由自动运行对应查询，回测路由恢复起始日期、跌幅和金额，周期路由使用真实周期刻度组件。

每条路由在不执行 JavaScript 时也能读取独立的问题说明、筛选公式、数据来源和预测边界，减少六个近似首页造成的重复内容。

### 搜索基础

- 首页和方法页具有 canonical。
- 工具路由具有独立 title、description、canonical、Open Graph 和 Twitter 元数据。
- `robots.txt` 公开 sitemap 地址。
- `sitemap.xml` 包含首页、方法页、数据状态页、工具目录和六个工具路由。

### 结构化数据

- 首页发布 WebApplication 数据。
- 工具目录发布 CollectionPage 数据。
- 每个工具路由发布自身 URL、名称和描述对应的 WebApplication 与 BreadcrumbList 数据。
- 线上逐项确认六个工具页结构化数据 URL 与 canonical 一致，并可返回工具目录。

### 功能与移动端

- 自动回归测试覆盖预设参数和分享参数。
- 浏览器验证 `SOL + 上涨5%` 查询可恢复并运行。
- 浏览器验证回测路由可恢复 `2020-01-01 / 5% / 100美元`。
- Playwright 覆盖 360、390、768 和 1440 四种宽度，工具目录无横向溢出，六个入口均可见。
- 完整数据与页面回归通过，浏览器门禁 20/20 通过。

### 线上部署验收

- 部署提交：`df6b855`。
- `https://www.mybtcbox.com/tools/` 返回 200，并显示独立工具目录内容。
- 六个工具路由全部返回 200。
- 六个工具路由逐项检查 canonical、静态计算口径、BreadcrumbList 和目录返回链接，全部通过。

## 当前评分

SEO 获客：8.5/10。

独立内容、发现路径、内部链接、结构化数据和响应式门禁已通过。尚无搜索平台与真实流量结果，因此不超过 8.5。

核心工具价值：8.5/10。用户可以从明确任务目录进入六个真实工具，但仍缺少任务完成率与使用时间证据。

数据表达：8.5/10。每个工具都公开自身公式、样本边界与来源，但仍需全部关键指标的可复算示例和跨来源抽查。

## 尚缺证据

- Google Search Console 或其他搜索平台的实际抓取与收录记录。
- 独立工具页的 Core Web Vitals 或 Lighthouse 证据。
- 搜索展示错误、重复内容和结构化数据错误的持续监控。
- 至少30天的自然搜索曝光、点击和有效工具使用数据。
