# 报告统计核心验收记录：2026-08-11

## 独立统计

`assets/report-core.js` 不访问页面或浏览器存储，独立计算：

- 样本期最高价、最低价、最大单日上冲和最大单日下探。
- 总收益、平均日涨跌和平均绝对波动。
- 七档日涨跌分布，所有有效日线只归入一个区间。
- 最长连续上涨与下跌，以及不同持续天数的次数。
- 年度收益、日波动、上涨天数、下跌天数和 YTD 标识。
- 超过 20% 的重大回撤、最深回撤和高点到底部天数。
- 月度平均收益、上涨月份比例和有效样本数。

## 确定性复算

测试使用跨 2023、2024、2025 的小型日线样本，验证：

- 年度收益使用上一年最后收盘作为完整年度基准。
- 月度收益使用上一月最后收盘作为基准。
- 未恢复的重大回撤只计为一个连续回撤期。
- 空数据返回完整的中性结构，不产生 `NaN` 或异常。
- 区间边界不会重复计数或漏计。

## 浏览器验收

2026-08-11 使用 DOGE 历史下跌查询生成报告：

- 报告弹窗成功打开，正文约 1,669 个字符。
- 极端事件、涨跌幅分布、年度表现和重大回撤均正常呈现。
- 报告样本数、1日、7日和30日结果继续展示有效样本分母。
- 页面无控制台错误，下载、复制和社交分享按钮仍保留。

## 真实导出与失败降级

- `html2canvas` 已改为用户点击下载后才加载，首屏与仅打开报告时都不请求该库。
- Playwright 在 390px、2 倍 DPR 的标准手机项目中运行真实 `html2canvas`，捕获浏览器下载并解析 PNG 文件头。
- 导出文件必须具有有效 PNG 签名、宽度至少 850 像素、高度大于宽度且体积超过 100KB，避免生成空图、缩略图或不可读长图。
- 报告弹窗可读性、视口边界和按需加载仍在 360、390、768、1440 四种宽度验证。
- 每日观察系统分享不可用且剪贴板拒绝时，页面明确提示“复制失败，请使用浏览器分享菜单”，不会静默失败。
- 完整浏览器套件结果：37 通过、3 个非 390px 的重复长图渲染按设计跳过；唯一真实长图验收通过。

## 当前评分

报告与分享：9.5/10。

报告内容、统计、跨端布局、真实 PNG 文件和分享失败降级均有自动证据。10 分仍需 X、微博、微信等真实平台预览保持 30 天稳定。
