# My BTC Box 工作交接

交接时间：2026-08-13（Asia/Shanghai）  
仓库：`D:\mybtcbox`  
线上：`https://www.mybtcbox.com/`  
GitHub：`https://github.com/huachuanfang-cmyk/btc-pattern`  
分支：`main`  
交接基线提交：`cc689ed`

## 1. 产品使命

My BTC Box 是面向普通用户的 BTC / 加密货币历史数据工具站。核心承诺：

- 不预测下一次涨跌，不给买卖指令。
- 用公开历史日线帮助用户查事件、做比较、跑回测、生成报告。
- 所有结论必须同时公开数据源、UTC 时间边界、有效样本数、日期范围、计算口径和限制。
- 周期数据只做历史时间对照，不得包装成“见底倒计时”“逃顶日期”或确定性预测。

任何新功能如果不能帮助用户完成“查数据、做对比、数据参考、数据分析、数据分享”中的至少一项，不应进入首页。

## 2. 当前真实状态

`quality-scorecard.json` 是唯一当前评分来源，`docs/quality-standard-95.md` 是评分标准。

| 维度 | 当前分 | 状态 |
| --- | ---: | --- |
| 产品定位 | 9.0 | 缺至少 5 名真实目标用户的 10 秒理解测试 |
| 核心工具价值 | 9.5 | 四项任务各 20 次，成功率 100%，p95 低于 1 秒 |
| 数据表达 | 9.5 | 有公式、公开复算 JSON、黄金样本和跨来源抽查 |
| 报告与分享 | 9.5 | 真实 PNG、移动端和失败降级已验收 |
| 移动端 | 9.5 | 360/390/768/1440 与性能预算已通过 |
| 数据正确性 | 9.5 | 黄金样本、确定性计算、Yahoo/Kraken 抽查已通过 |
| 用户粘性 | 8.5 | 基础设施完成，缺真实 D1/D7/D30 和主动订阅长期证据 |
| SEO 获客 | 9.0 | 首页已能公开搜索到，缺核心工具页收录和 30 天数据 |
| 数据运维 | 9.5 | 两条真实告警均完成机器人自动恢复关闭 |
| 代码扩展性 | 9.5 | 有模块和架构预算，但 `assets/app.js` 已使用 93.4% 预算 |

严禁把产品定位、用户粘性或 SEO 直接改成 9.5。它们缺的是现实世界证据，不是再写一段代码。

## 3. 已验证的生产闭环

- 最近一次完整日更成功运行：GitHub Actions `31690876268`，并生成提交 `a110521`。
- 最近一次质量门禁成功运行：`31691074081`。
- 线上健康告警 [Issue #1](https://github.com/huachuanfang-cmyk/btc-pattern/issues/1) 已由机器人恢复并关闭。
- 日更失败告警 [Issue #2](https://github.com/huachuanfang-cmyk/btc-pattern/issues/2) 已由机器人恢复并关闭。
- 当前 GitHub 推送和自动部署可用；GitHub CLI 本机可能仍未登录，不要把浏览器登录等同于 CLI 登录。

## 4. 开工前必须阅读

按顺序完整阅读：

1. `HANDOFF.md`
2. `docs/quality-standard-95.md`
3. `quality-scorecard.json`
4. `docs/product-priorities.md`
5. `docs/roadmap-95.md`
6. `docs/real-world-validation.md`
7. 与当前任务对应的 `docs/evidence/*.md`

然后执行：

```powershell
git status -sb
git pull --ff-only origin main
npm test
npm run test:performance
```

如果基线测试失败，先报告失败，不得在未知基线上继续堆功能。

## 5. 文件所有权和安全边界

以下两个未跟踪文件属于用户，不得查看内容、修改、删除、暂存或提交：

- `wechat-ai-transition-article.html`
- `worker.js`

其他规则：

- 不使用 `git reset --hard`、强制推送或覆盖历史。
- 每次只提交当前任务明确涉及的文件，禁止 `git add .`。
- 推送前执行 `git diff --check`、完整测试和相关专项测试。
- 数据更新机器人可能向 `main` 追加数据提交；提交前重新 `git fetch`，只允许 fast-forward 同步。
- 不在仓库、日志、Issue 或报告中写入令牌、Cookie、邮箱、钱包地址或其他凭据。
- 不新增浏览器直连任意代理，不绕过 `/api/market` 白名单。
- 不降低性能、架构、正确性或样本校验门槛来让测试通过。

## 6. 评分纪律（防止模型虚假满分）

只有同时满足以下条件才允许修改 `quality-scorecard.json`：

1. `docs/quality-standard-95.md` 对应验收点确实满足。
2. 有新的仓库证据文件、自动测试、线上响应或真实用户记录。
3. `docs/evidence/` 中写明时间、输入、方法、原始结果、限制和失败项。
4. `npm test` 中的评分门禁通过。
5. 不把“功能存在”当作“用户有效使用”，不把自动测试当作真实用户或 30 天证据。

不得使用“看起来很好”“功能齐全”“模型自评通过”作为加分理由。证据不足就保持原分，并在 `nextMilestone` 中记录缺口。

## 7. 交接期间允许自主推进的任务

下面任务风险较低、边界明确，可以依次执行。一次只做一项，完成、测试、提交、上线验证后再开始下一项。

### A. 建立线上发布验收脚本（优先）

目标：新增 `scripts/check-online-release.js` 和 npm 命令 `verify:online`，只读检查：

- 首页、工具目录、方法页、状态页、隐私页和六个工具 URL 返回 200。
- HTML 路由 Content-Type 为 HTML；五份日线、健康清单和公开复算样本为 JSON。
- 每个工具 URL 的 canonical、title、静态口径和 JSON-LD URL 与当前地址一致。
- 首页包含“不预测”边界；方法页包含 Dataset；隐私页说明默认关闭分析。
- 健康清单 5/5 且最大 UTC 延迟不超过 2 天。

要求：网络失败要给出具体 URL 和原因；不得因为临时网络失败改低门槛；给脚本写离线单元测试或可注入 fetch 的测试。该任务完成后不改变任何评分。

### B. 固化真实用户测试记录模板

根据 `docs/real-world-validation.md` 新增空白模板，例如 `docs/templates/user-test-template.csv` 和一页填写说明。字段不能包含姓名、联系方式、钱包或设备指纹。不得自己编造 5 名用户数据，不得因此提高产品定位分数。

### C. 建立 30 天证据采集模板

新增只含字段定义的模板，用于每周记录：GitHub 日更成功率、线上巡检成功率、恢复次数、搜索收录数量、自然曝光、D1/D7/D30 聚合数量。不要填假数据，不要把尚未成熟的用户放入 D7 分母。

### D. 小型无障碍检查

只做可自动验证的改进：键盘焦点、按钮可访问名称、颜色对比、44px 点击区域、弹窗关闭与 Esc。必须在四种 Playwright 项目通过。不要大改视觉系统，不提高评分。

## 8. 暂停，等待 Codex 恢复额度后再做

这些任务需要较强产品或架构判断，其他模型不要自主执行：

- 大规模拆分 `assets/app.js` 或改变全局状态初始化顺序。
- 新增币种、技术指标、AI 分析、价格预测、综合评分或交易信号。
- 修改周期高低点日期、回撤算法、事件阈值和回测成交假设。
- 接入推送通知、邮件、账号、数据库、付费、钱包连接或新的第三方分析平台。
- 重做首页信息架构、品牌视觉或报告设计。
- 删除旧功能、改域名、改托管平台、改 GitHub Actions 权限。
- 调高任何低于 9.5 的评分。

若发现严重数据错误、安全问题或线上核心工具不可用，可以修复，但必须先保存复现证据和失败测试。

## 9. 每项工作的完成定义

一项任务只有满足全部条件才算完成：

- 需求对应的测试先失败、修改后通过，或明确记录为何无法先写失败测试。
- `npm test` 通过。
- `npm run test:performance` 通过。
- 涉及 UI 时 `npm run test:e2e` 通过。
- `git diff --check` 通过。
- 只暂存任务文件，提交信息准确。
- 推送 `main` 成功，等待 GitHub 质量门禁成功。
- 用线上 URL 验证部署内容，不把 `git push` 当作上线完成。
- 在交付说明中列出：改了什么、没有改什么、测试结果、线上结果、遗留风险。

## 10. 当前已知注意点

- `assets/app.js` 约 140KB / 150KB，禁止继续大量塞逻辑；新增独立模块也必须设置架构预算。
- 首屏性能预算：请求不超过 24、传输不超过 1.25MB、LCP 小于 2500ms、CLS 小于 0.1，首屏不得加载 html2canvas。
- 日线计算以 Yahoo Finance USD 交易对 UTC 完整日线为主；当日未完成日线必须排除。
- 实时行情优先 CoinGecko，失败降级 CoinLore；实时值不替代完整日线统计。
- 黄金样本只能经人工核对上游修订后明确更新，禁止更新脚本自动改写。
- 分析默认关闭。用户主动同意后才加载 GA；不得上传查询明细、币种偏好、钱包或报告内容。
- 搜索公开结果目前能找到首页，但抓取快照可能滞后；不得把 `site:` 搜索当作 Search Console 完整证据。

## 11. 回交给 Codex 时提供

请提交一份简洁回交说明，包含：

- 起止提交 SHA。
- 每个提交的目的和文件列表。
- 完整命令及通过/失败数量。
- GitHub Actions 运行 ID 和结论。
- 线上逐 URL 验证结果。
- 未解决问题和没有执行的高风险事项。
- `git status -sb` 原样输出。

